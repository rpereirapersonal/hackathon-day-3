# Pinned so the setup procedure reproduces from a clean checkout (NFR-5.1).
# Verify the whole set installs on the host's aarch64 architecture before the
# data layer is built (DEP-4, build step 0) — onnxruntime, pulled in by
# fastembed, is the one most likely to lack a wheel.

langchain~=1.3
langchain-openai~=1.2
langgraph~=1.1
langgraph-cli[inmem]~=0.4

fastapi~=0.136
uvicorn[standard]~=0.38
pydantic~=2.9

# Data layer (BLK-2)
aiosqlite~=0.22
fastembed>=0.7,<1.0

pytest~=8.3
pytest-asyncio~=0.24
httpx~=0.28
